<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.momo.review.mapper.ReviewMapper"> 
  <insert id="insert" parameterType="map">
  	INSERT INTO review(review_no, review_rating, review_content, review_writer, user_id, review_date) VALUES ( #{reviewNo}, #{reviewRating}, #{reviewContent}, #{reviewWriter}, #{userId}, #{writingDate})
  </insert>
</mapper>
   